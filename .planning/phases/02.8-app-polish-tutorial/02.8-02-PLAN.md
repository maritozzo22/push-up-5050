---
phase: 02.8-app-polish-tutorial
plan: 02
type: execute
wave: 2
depends_on: ["02.8-01"]
files_modified:
  - push_up_5050/lib/screens/series_selection/series_selection_screen.dart
  - push_up_5050/lib/screens/statistics/statistics_screen.dart
autonomous: false

must_haves:
  truths:
    - "No bottom overflow errors on Series Selection screen"
    - "No bottom overflow errors on Statistics screen"
    - "All content remains accessible with SafeArea on small screens"
    - "Scrollable content works properly without layout breakage"
  artifacts:
    - path: "push_up_5050/lib/screens/series_selection/series_selection_screen.dart"
      provides: "Fixed SeriesSelectionScreen with scrollable content"
      min_lines: 400
    - path: "push_up_5050/lib/screens/statistics/statistics_screen.dart"
      provides: "Fixed StatisticsScreen (already scrollable, verify padding)"
      min_lines: 200
  key_links:
    - from: "push_up_5050/lib/screens/series_selection/series_selection_screen.dart"
      to: "flutter SingleChildScrollView"
      via: "Wrap Column in SingleChildScrollView for overflow fix"
      pattern: "SingleChildScrollView.*physics.*AlwaysScrollableScrollPhysics"
</context>

<objective>
Fix RenderFlex bottom overflow errors on Series Selection and Statistics screens.

Purpose: User-reported overflow issues make content inaccessible on smaller screens. SingleChildScrollView wrapping ensures all content remains accessible while preserving design.

Output: Overflow issues resolved, screens scroll properly on all device sizes.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02.8-app-polish-tutorial/02.8-CONTEXT.md
@.planning/phases/02.8-app-polish-tutorial/02.8-RESEARCH.md

@push_up_5050/lib/screens/series_selection/series_selection_screen.dart
@push_up_5050/lib/screens/statistics/statistics_screen.dart
</context>

<tasks>

<task type="auto">
  <name>Task 1: Analyze overflow root cause in Series Selection screen</name>
  <files>push_up_5050/lib/screens/series_selection/series_selection_screen.dart</files>
  <action>
Read the current SeriesSelectionScreen layout structure.

Identify:
1. The Column inside SafeArea (lines ~302-413)
2. Fixed heights on _FrostCardStepper widgets (140px, 160px, 160px)
3. Spacer() widget pushing content to bottom
4. No SingleChildScrollView wrapper

Root cause: Column content + fixed heights + Spacer() exceeds screen height on small devices. The screen lacks scroll capability.
  </action>
  <verify>Screen structure identified, Column children counted</verify>
  <done>Overflow root cause understood - need to wrap Column in SingleChildScrollView</done>
</task>

<task type="auto">
  <name>Task 2: Fix Series Selection screen overflow</name>
  <files>push_up_5050/lib/screens/series_selection/series_selection_screen.dart</files>
  <action>
Modify the SeriesSelectionScreen build() method:

1. Wrap the existing Column in a SingleChildScrollView
2. Keep SafeArea as outer container
3. Remove Spacer() and replace with fixed SizedBox(height: 24) at bottom
4. Ensure physics: AlwaysScrollableScrollPhysics() for consistent scroll behavior

Structure becomes:
```
Scaffold
  Stack
    AppBackground
    SafeArea
      SingleChildScrollView  // ADD THIS
        physics: AlwaysScrollableScrollPhysics()
        child: Padding
          child: Column  // Existing content
            // Top bar
            // Title
            // Starting Series Card
            // Rest Time Card
            // Goal Card
            SizedBox(height: 24)  // REPLACE Spacer
            // BEGIN WORKOUT Button
            SizedBox(height: 24)
```

DO NOT change:
- Card heights (keep 140, 160, 160)
- Card styling and layout
- Button styles
- Color scheme
  </action>
  <verify>SingleChildScrollView wraps Column content, Spacer replaced with SizedBox</verify>
  <done>SeriesSelectionScreen has scrollable content, no overflow on small screens</done>
</task>

<task type="auto">
  <name>Task 3: Verify Statistics screen overflow fix</name>
  <files>push_up_5050/lib/screens/statistics/statistics_screen.dart</files>
  <action>
Read StatisticsScreen and verify it already has SingleChildScrollView.

The screen should already have:
- SafeArea
- SingleChildScrollView with AlwaysScrollableScrollPhysics
- Proper padding

If SingleChildScrollView exists, no changes needed.
If missing, add it following the same pattern as SeriesSelection.

Note: StatisticsScreen (lines 54-67) already wraps content in SingleChildScrollView with proper padding. Verify this is still the case and confirm no changes are needed.
  </action>
  <verify>StatisticsScreen has SingleChildScrollView wrapper</verify>
  <done>Statistics screen overflow verified (already has scroll, or fix applied)</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Fixed Series Selection and Statistics screens with scrollable content</what-built>
  <how-to-verify>
1. Rebuild app: `cd push_up_5050 && flutter build apk --debug`
2. Install on emulator: `flutter install`
3. Launch app and navigate to Series Selection (tap START)
4. Verify:
   - All cards visible and scrollable if needed
   - "BEGIN WORKOUT" button accessible
   - No overflow error banners
   - Scrolling works smoothly
5. Navigate to Statistics tab
6. Verify:
   - All stats cards visible
   - Calendar at bottom accessible
   - No overflow errors
   - Scrolling works if content exceeds screen

Try reducing emulator screen size to smallest available to test edge cases.
  </how-to-verify>
  <resume-signal>Type "approved" if no overflow errors, or describe which screen still has issues</resume-signal>
</task>

</tasks>

<verification>
- SeriesSelectionScreen uses SingleChildScrollView
- StatisticsScreen has or gains SingleChildScrollView
- No Spacer widgets causing overflow (replaced with fixed spacing)
- All content remains accessible on small screens
- Scrolling works properly with AlwaysScrollableScrollPhysics
</verification>

<success_criteria>
- App builds and installs without errors
- No RenderFlex overflow errors on Series Selection screen
- No RenderFlex overflow errors on Statistics screen
- All content accessible via scrolling on small screens
- User confirms screens work correctly on emulator
</success_criteria>

<output>
After completion, create `.planning/phases/02.8-app-polish-tutorial/02.8-02-SUMMARY.md`
</output>
