---
phase: 06-statistics-ui-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - push_up_5050/lib/widgets/statistics/total_pushups_card.dart
autonomous: true

must_haves:
  truths:
    - "TotalPushupsCard displays without progress bar at bottom"
    - "TotalPushupsCard displays without percentage text"
    - "Card maintains 120px height and frost glass styling"
    - "Total/goal values remain visible and centered"
  artifacts:
    - path: push_up_5050/lib/widgets/statistics/total_pushups_card.dart
      provides: "Total pushups display without progress bar"
      contains: "class TotalPushupsCard"
      not_contains: "ProgressBar"
  key_links:
    - from: push_up_5050/lib/widgets/statistics/total_pushups_card.dart
      to: push_up_5050/lib/widgets/design_system/frost_card.dart
      via: "FrostCard wrapper"
      pattern: "FrostCard\\("
---

<objective>
Remove the progress bar and percentage display from the TotalPushupsCard widget.

The progress bar and percentage text at the bottom of the TotalPushupsCard clutter the UI and are unnecessary since the user can see their total/goal values directly. Removing these elements simplifies the visual hierarchy and prepares the card for the next plan which will center and enlarge the text.

Output: Updated TotalPushupsCard without progress bar and percentage display.
</objective>

<execution_context>
@C:\Users\olegn\.claude\get-shit-done/workflows/execute-plan.md
@C:\Users\olegn\.claude\get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md

# Current implementation
@push_up_5050/lib/widgets/statistics/total_pushups_card.dart
</context>

<tasks>

<task type="auto">
  <name>Remove progress bar and percentage from TotalPushupsCard</name>
  <files>push_up_5050/lib/widgets/statistics/total_pushups_card.dart</files>
  <action>
In `total_pushups_card.dart`, remove the following elements from the Column children:

1. Remove the `Spacer()` widget (line 85)
2. Remove the `ProgressBar(value: progress)` widget (line 87)
3. Remove the `SizedBox(height: 2)` after progress bar (line 88)
4. Remove the percentage Text widget showing '$percentage%' (lines 89-97)

After removal, the Column should have:
- Icon circle container
- SizedBox(height: 4)
- "TOTALE PUSHUPS" label Text
- SizedBox(height: 2)
- "$total / $goal" value FittedBox
- No spacer, no progress bar, no percentage text

Also remove unused imports and variables:
- Remove the `progress_bar.dart` import (line 3)
- Remove the `progress` variable calculation (line 27)
- Remove the `percentage` variable calculation (line 28)

DO NOT change:
- Card height (120px)
- Icon styling
- Label text ("TOTALE PUSHUPS")
- Value display format ("$total / $goal")
  </action>
  <verify>flutter test</verify>
  <done>
TotalPushupsCard displays with:
- Icon at top
- Label "TOTALE PUSHUPS"
- Value "$total / $goal"
- NO progress bar at bottom
- NO percentage text
  </done>
</task>

</tasks>

<verification>
After applying changes:

1. Run `flutter test` to verify no existing tests break
2. Verify `TotalPushupsCard` builds without the ProgressBar import
3. Confirm card height remains 120px
4. Confirm all text elements remain visible and properly aligned
</verification>

<success_criteria>
1. TotalPushupsCard compiles without errors
2. Progress bar is removed from card layout
3. Percentage text is removed from card layout
4. Card maintains visual consistency with other statistics cards
5. All existing tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/06-statistics-ui-polish/06-01-SUMMARY.md`
</output>
