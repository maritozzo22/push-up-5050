---
phase: 06-statistics-ui-polish
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - push_up_5050/lib/widgets/statistics/total_pushups_card.dart
  - push_up_5050/lib/widgets/statistics/calorie_card.dart
autonomous: true

must_haves:
  truths:
    - "TotalPushupsCard label is larger than current 8px font size"
    - "TotalPushupsCard value is larger than current 13px font size"
    - "CalorieCard label is larger than current 8px font size"
    - "CalorieCard value is larger than current 13px font size"
    - "Both cards have vertically centered content within 120px height"
    - "Font sizes are consistent between both cards"
  artifacts:
    - path: push_up_5050/lib/widgets/statistics/total_pushups_card.dart
      provides: "Total pushups display with enlarged centered text"
      contains: "fontSize: 10"
      contains: "fontSize: 16"
    - path: push_up_5050/lib/widgets/statistics/calorie_card.dart
      provides: "Calorie display with enlarged centered text"
      contains: "fontSize: 10"
      contains: "fontSize: 16"
  key_links:
    - from: push_up_5050/lib/widgets/statistics/total_pushups_card.dart
      to: push_up_5050/lib/widgets/statistics/calorie_card.dart
      via: "Consistent font size pattern"
      pattern: "fontSize:\\s+(10|16)"
---

<objective>
Center and enlarge text in both TotalPushupsCard and CalorieCard for improved readability and visual hierarchy.

The current 8px label and 13px value fonts are too small for comfortable reading. By increasing the label to 10px and value to 16px, and adjusting vertical alignment, the cards become more readable while maintaining the compact form factor.

Output: Updated TotalPushupsCard and CalorieCard with larger, centered text.
</objective>

<execution_context>
@C:\Users\olegn\.claude\get-shit-done/workflows/execute-plan.md
@C:\Users\olegn\.claude\get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md

# Current implementation (after 06-01)
@push_up_5050/lib/widgets/statistics/total_pushups_card.dart
@push_up_5050/lib/widgets/statistics/calorie_card.dart

# Prior plan
@.planning/phases/06-statistics-ui-polish/06-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Enlarge text in TotalPushupsCard</name>
  <files>push_up_5050/lib/widgets/statistics/total_pushups_card.dart</files>
  <action>
In `total_pushups_card.dart`, update font sizes for better readability:

1. Update the "TOTALE PUSHUPS" label Text widget (around line 62):
   - Change `fontSize: 8` to `fontSize: 10`

2. Update the "$total / $goal" value Text widget inside FittedBox (around line 78):
   - Change `fontSize: 13` to `fontSize: 16`

3. Add `mainAxisAlignment: MainAxisAlignment.center` to the Column (around line 33):
   - This centers content vertically within the 120px card height

DO NOT change:
- Icon styling (32px circle, 16px icon)
- Card height (120px)
- Text content or labels
- Color values
- FontWeight values (w800, w900)
  </action>
  <verify>flutter test</verify>
  <done>
TotalPushupsCard displays with:
- Label at 10px (was 8px)
- Value at 16px (was 13px)
- Content vertically centered in card
  </done>
</task>

<task type="auto">
  <name>Enlarge text in CalorieCard</name>
  <files>push_up_5050/lib/widgets/statistics/calorie_card.dart</files>
  <action>
In `calorie_card.dart`, update font sizes to match TotalPushupsCard:

1. Update the "CALORIE BRUCIATE" label Text widget (around line 64):
   - Change `fontSize: 8` to `fontSize: 10`

2. Update the "$kcal kcal" value Text widget inside FittedBox (around line 80):
   - Change `fontSize: 13` to `fontSize: 16`

3. Add `mainAxisAlignment: MainAxisAlignment.center` to the Column (around line 35):
   - This centers content vertically within the 120px card height

4. Adjust the final `SizedBox(height: 8)` at bottom (line 87):
   - Remove it or reduce to `SizedBox(height: 4)` since MainAxisAlignment.center handles spacing

DO NOT change:
- Icon styling (32px circle, 16px icon)
- Card height (120px)
- Text content or labels
- Color values
- FontWeight values (w800, w900)
  </action>
  <verify>flutter test</verify>
  <done>
CalorieCard displays with:
- Label at 10px (was 8px)
- Value at 16px (was 13px)
- Content vertically centered in card
- Consistent styling with TotalPushupsCard
  </done>
</task>

</tasks>

<verification>
After applying changes:

1. Run `flutter test` to verify no existing tests break
2. Verify both cards have label at 10px and value at 16px
3. Verify content is vertically centered within 120px card height
4. Confirm visual consistency between TotalPushupsCard and CalorieCard
5. Test on Chrome/web to verify visual appearance
</verification>

<success_criteria>
1. Both cards compile without errors
2. Label text increased from 8px to 10px in both cards
3. Value text increased from 13px to 16px in both cards
4. Content is vertically centered in both cards
5. Both cards maintain consistent visual styling
6. All existing tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/06-statistics-ui-polish/06-02-SUMMARY.md`
</output>
