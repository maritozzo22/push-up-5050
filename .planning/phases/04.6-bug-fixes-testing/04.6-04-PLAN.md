---
phase: 04.6-bug-fixes-testing
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - push_up_5050/lib/widgets/statistics/calorie_card.dart
autonomous: true

must_haves:
  truths:
    - "Calorie card displays without unnecessary gradient bar at bottom"
    - "Calorie card visual design matches other stat cards (TotalPushupsCard)"
    - "Card maintains proper spacing and layout after bar removal"
  artifacts:
    - path: "push_up_5050/lib/widgets/statistics/calorie_card.dart"
      provides: "Clean calorie display without decorative bar"
      contains: "CalorieCard|Column"
  key_links:
    - from: "calorie_card.dart"
      to: "total_pushups_card.dart (reference)"
      via: "Similar layout structure without bottom bar"
      pattern: "no Spacer|Container.*gradient"
---

<objective>
Fix BUG-04: Calorie card has unnecessary gradient bar at bottom.

Currently, CalorieCard widget has a decorative gradient bar at the bottom (lines 88-99) that other stat cards like TotalPushupsCard don't have. This adds visual clutter and inconsistent design across stat cards.

Purpose: Provide consistent visual design across all stat cards by removing the unnecessary decorative element.
Output: Clean CalorieCard matching the design of other stat cards.
</objective>

<execution_context>
@C:\Users\olegn\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\olegn\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/phases/04.6-bug-fixes-testing/04.6-RESEARCH.md
@.planning/ROADMAP.md
@.planning/STATE.md
@push_up_5050/lib/widgets/statistics/calorie_card.dart
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove unnecessary gradient bar from CalorieCard</name>
  <files>push_up_5050/lib/widgets/statistics/calorie_card.dart</files>
  <action>
    In calorie_card.dart, remove the decorative gradient bar at the bottom:

    1. Locate lines 87-100 containing the Spacer and Container:
       ```dart
       const Spacer(),
       // Small indicator bar at bottom
       Container(
         height: 3,
         decoration: BoxDecoration(
           gradient: const LinearGradient(...),
           borderRadius: BorderRadius.circular(2),
         ),
       ),
       ```

    2. Remove both the `const Spacer(),` and the `Container(...)` widget.

    3. Replace with simple spacing if needed for layout balance:
       ```dart
       const SizedBox(height: 8),
       ```

    4. The Column children should now be:
       - Icon circle container (lines 38-61)
       - SizedBox(height: 4)
       - Label text "CALORIE BRUCIATE" (lines 64-73)
       - SizedBox(height: 2)
       - Value text "$kcal kcal" (lines 76-85)
       - SizedBox(height: 8) [replaces Spacer + Container]

    DO NOT modify: Icon circle, label, value, or card height (120).
  </action>
  <verify>Search for "Spacer()" in calorie_card.dart - should NOT exist. Search for "Container" within the Column - only the icon circle Container should exist (no gradient bar).</verify>
  <done>Calorie card displays cleanly without unnecessary decorative gradient bar.</done>
</task>

</tasks>

<verification>
1. Run `flutter analyze` - no errors in calorie_card.dart
2. Verify no Spacer() in the Column children
3. Check that only one Container exists (the icon circle)
4. Build renders without layout issues
</verification>

<success_criteria>
1. Spacer() removed from CalorieCard Column
2. Gradient bar Container removed from CalorieCard
3. Simple SizedBox(height: 8) provides bottom spacing
4. Card maintains 120px height and proper alignment
</success_criteria>

<output>
After completion, create `.planning/phases/04.6-bug-fixes-testing/04.6-04-SUMMARY.md`
</output>
